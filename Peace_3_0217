The arrays are used for allowing objects to be initialized rendered and updated.

The code for an int array is int[] anArray;


The code for a String array is private String name = new String[]

The code for a boolean is static boolean = newcar
 new car = False

The code for a double array is int[]][] int A

To change the first value of the int to 42 you must have the code anArray = 42

To change the value of a String to Bob you must put anArray = Bob

That adds three postres.
That  gives the postres names.

You change the array for bunuelos to apple pie.


